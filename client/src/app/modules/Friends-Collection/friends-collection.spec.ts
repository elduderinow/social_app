import { FriendsCollection } from './friends-collection';

describe('FriendsCollection', () => {
  it('should create an instance', () => {
    expect(new FriendsCollection("",[],[])).toBeTruthy();
  });
});
